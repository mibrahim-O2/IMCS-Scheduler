# Phase 0: API scaffold
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# Phase 1: database models, migrations and seed data
sqlalchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.5

# Later phases, uncomment when the phase that needs them begins
# python-docx            # Word export (PROJECT_ARCHITECTURE.md §9)
# python-multipart       # course scheme file uploads (§7)
# pytest                 # scheduler and API tests
# httpx                  # FastAPI TestClient dependency
