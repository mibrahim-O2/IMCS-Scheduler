# Phase 0: API scaffold
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# Phase 1: database models, migrations and seed data
sqlalchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.5

# Phase 2: course scheme upload, extraction and storage
python-multipart==0.0.32   # multipart file uploads
pdfplumber==0.11.10        # PDF text-layer extraction
python-docx==1.2.0         # Word (.docx) text extraction
httpx==0.28.1              # Supabase Storage REST calls
pytesseract==0.3.13        # OCR fallback for scanned PDFs (needs the Tesseract binary)
pdf2image==1.17.0          # rasterizes PDF pages for OCR (needs poppler)

# Later phases, uncomment when the phase that needs them begins
# pytest                 # scheduler and API tests
# reportlab              # PDF export (PROJECT_ARCHITECTURE.md §9)
