"""Hard constraints (heavy penalty), one function per rule.

Teacher double-booking (checked globally, across programs), room
double-booking, cross-part teacher clash, teaching outside declared
availability, and lab-batch clashes within a split. See docs/PROJECT_ARCHITECTURE.md §6.2.
"""
