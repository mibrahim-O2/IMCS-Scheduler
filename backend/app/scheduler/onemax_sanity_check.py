"""One Max sanity check for the GA machinery (scaffolding, not a feature).

Will evolve a random bit-string to all 1s using operators.py, proving the
selection/crossover/mutation loop works before the real timetable chromosome
is wired in. To be removed or moved to tests/ afterwards. See docs/PROJECT_ARCHITECTURE.md §6.4.
"""
