"""Teacher model, resolved by full name (never by initials), with declared availability.

Cross-program load is derived from TimetableSession rows, not restricted
here. See docs/PROJECT_ARCHITECTURE.md §3.4.
"""
